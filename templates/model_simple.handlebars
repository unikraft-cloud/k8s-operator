{{#if unescapedDescription}}
// {{{ replace unescapedDescription "
" "
// "}}}
{{~/if}}

{{~#each vars}}
{{~#unless deprecated}}
{{~#if isEnum}}
{{~#if unescapedDescription}}
// {{{ replace unescapedDescription "
" "
	// "}}}
{{~/if}}
// +kubebuilder:validation:Enum={{#each allowableValues.values}}{{#if @first}}{{.}}{{else}};{{.}}{{/if}}{{/each}}
type {{classname}}{{name}} string

const (
{{~#each allowableValues.values}}
	{{classname}}{{name}}{{replace (capitalize .) "-" "_"}} {{classname}}{{name}} = "{{.}}"
{{~/each}}
)
{{~/if}}
{{~/unless}}
{{~/each}}

type {{classname}} struct {
{{~#if (and (not allOf) composedSchemas.allOf)}}
{{~#each composedSchemas.allOf}}
{{~#each composedSchemas.oneOf}}
{{~#each vars}}
{{~#if deprecated}}
	// Deprecated.
{{~/if}}
{{~#if unescapedDescription}}
	// {{{ replace unescapedDescription "
" "
	// "}}}
{{~/if}}
	{{name}} *{{#if isEnum}}{{#if isArray}}[]{{/if}}{{classname}}{{name}}{{~else}}{{#if (eq openApiType "integer")}}{{{dataFormat}}}{{else}}{{dataType}}{{/if}}{{/if}} `json:"{{baseName}},omitempty"{{#with withXml}} xml:"{{baseName}}{{#if isXmlAttribute}},attr{{/if}}"{{/with}}{{#with vendorExtensions.x-go-custom-tag}} {{{.}}}{{/with}}`
{{~/each}}
{{~/each}}
{{~/each}}
{{~else}}
{{~#each vars}}
{{~#if deprecated}}
	// Deprecated.
{{~/if}}
{{~#if unescapedDescription}}
	// {{{ replace unescapedDescription "
" "
	// "}}}
{{~/if}}
	{{name}} {{#unless required}}{{#unless (or isArray (eq openApiType "object"))}}*{{/unless}}{{/unless}}{{#if isEnum}}{{#if isArray}}[]{{/if}}{{classname}}{{name}}{{~else}}{{#if (eq openApiType "integer")}}{{{dataFormat}}}{{else}}{{dataType}}{{/if}}{{/if}} `json:"{{baseName}}{{#unless required}},omitempty{{/unless}}"{{#with withXml}} xml:"{{baseName}}{{#if isXmlAttribute}},attr{{/if}}"{{/with}}{{#with vendorExtensions.x-go-custom-tag}} {{{.}}}{{/with}}`
{{~/each}}
{{~/if}}
}
